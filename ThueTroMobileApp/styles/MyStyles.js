import { StyleSheet } from "react-native";

export default StyleSheet.create ({
    container: {
        flex: 1,
        margin: 5
    }, subject: {
        fontSize: 30,
        fontWeight: "bold",
        textAlign: "center",
        color: "blue"
    }
});